kind: Namespace
apiVersion: v1
metadata:
  name: air-rockey-dashboard
  labels:
    name: air-rockey-dashboard
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: echo-ingress
  annotations:  
    certmanager.k8s.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  tls:
  - hosts:
    - rocket-dashboard.lappis.rocks
    secretName: letsencrypt-prod
  rules:
  - host: rocket-dashboard.lappis.rocks
    http:
      paths:
      - backend:
          serviceName: dashboard
          servicePort: 80
---
kind: Service
apiVersion: v1
metadata:
  name: dashboard-service
spec:
  selector:
    app: dashboard
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
  namespace: air-rockey-dashboard
  labels:
    app: dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      imagePullSecrets:
      - name: pipeline-docker-registry
      containers:
      - name: dashboard
        image: ${CICD_IMAGE}:${CICD_EXECUTION_SEQUENCE}
        ports:
        - containerPort: 8082